import { getIsSupportedPathLng } from './lib/get-is-supported-path-lng';
import { getLngPathSegmentServer } from './lib/get-lng-path-segment-server';
import i18next from './lib/i18next';
import { fallback_lng, header_lng_param_name, lngs } from './lib/i18next-settings';

export { getIsSupportedPathLng, getLngPathSegmentServer, i18next, fallback_lng, header_lng_param_name, lngs };
