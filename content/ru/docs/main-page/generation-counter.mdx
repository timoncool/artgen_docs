import { Callout } from '@/components/mdx'

# Счётчик генераций

![Счётчик генераций](/screens/user-stats.png)

Счётчик генераций отображает количество доступных генераций и помогает отслеживать ваш баланс.

<Callout type="info">
  Счётчик всегда виден в верхней части экрана, что позволяет быстро проверить остаток генераций.
</Callout>

## Расположение

Счётчик находится в верхней части интерфейса и всегда доступен для просмотра.

## Информация в счётчике

- **Текущий баланс** — количество доступных генераций
- **Тип генераций** — ежедневные или несгораемые
- **Статус подписки** — активна ли PRO подписка

## Цветовая кодировка

Счётчик использует цветовую маркировку для различения типов генераций:

- **Белый цвет** — ежедневные генерации. Это ваш стандартный дневной лимит:
  - 50 без подписки
  - 300 с PRO-подпиской
  - 600 с MAX-подпиской
- **Зелёный цвет** — несгораемые генерации. Бонусные или купленные генерации, которые не имеют срока действия.

<Callout emoji="💡">
Несгораемые генерации расходуются только после того, как закончатся ежедневные. Это позволяет экономить бонусные генерации для больших проектов.
</Callout>

## Обновление баланса

Баланс обновляется автоматически:

- После каждой генерации
- В начале нового дня (для ежедневных генераций)
- После покупки дополнительных генераций
